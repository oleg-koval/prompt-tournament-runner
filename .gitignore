node_modules/
dist/
coverage/
docs/api/
*.tsbuildinfo
.DS_Store
.env
.env.local

# local tournament data
.prompt-tournament-runs.json

# local model-connection config (no secrets, but machine-specific)
.prompt-tournament-config.json
